server:
  port: 5000

spring:
  cloud:
    config:
      name: config
      server:
        git:
          uri: https://github.com/buildandship/flight-ticket-system/
          #uri: file:/Users/praveen/iprojects/flight-ticket-system
          search-paths: config-files*
  application:
    name: config